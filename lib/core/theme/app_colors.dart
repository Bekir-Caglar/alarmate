import 'package:flutter/material.dart';

class AppColors {
  // Primary Blue Tones
  static const Color primary = Color(0xFF2563EB); // Royal Blue
  static const Color primaryLight = Color(0xFF60A5FA);
  static const Color primaryDark = Color(0xFF1E3A8A);
  
  // Secondary / Accent Colors
  static const Color accent = Color(0xFF38BDF8); // Light Sky Blue
  
  // Light Background Colors
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Colors.white;
  
  // Dark Background Colors
  static const Color backgroundDark = Color(0xFF0F172A); // Slate 900
  static const Color surfaceDark = Color(0xFF1E293B); // Slate 800
  
  // Text Colors (Common or Light mode)
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);

  // Text Colors (Dark mode)
  static const Color textDarkPrimary = Color(0xFFF1F5F9); // Slate 100
  static const Color textDarkSecondary = Color(0xFF94A3B8); // Slate 400
  
  // Status Colors
  static const Color error = Color(0xFFEF4444);
  static const Color success = Color(0xFF10B981);
  // Border and Shadow
  static const Color border = Color(0xFF000000);
  static const Color shadow = Color(0xFF000000);
  static const Color borderDark = Color(0xFF475569); // Slate 600
  static const Color shadowDark = Color(0xFF475569);
}
